interface:
  display_name: "From Issue"
  short_description: "Research an issue and ship a PR"
  default_prompt: "Use $from-issue to research this issue, check linked PRs, pause with an implementation checkpoint when the change is not obviously small, then implement, verify, and open a PR."
